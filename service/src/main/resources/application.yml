spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/massage_project
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
      default_schema: massage

logging:
  level:
    root: INFO
